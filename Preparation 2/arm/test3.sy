int main()
{
    int a, b, i, t, n;

    a = 0;
    b = 1;
    i = 1;
    n = getint();
    // putf("%d\n%d\n", a, b);
    putint(a);
    putint(b);
    while (i < n)
    {
        t = b;
        b = a + b;
        // putf("%d\n", b);
        putint(b);
        a = t;
        i = i + 1;
    }
    return 0;
}
